# 2026-09-15 图标生成记录

使用内置 image_gen 生成与编辑。系统圆角、阴影、缩放及 Dock 对比图为 AI 视觉模拟，不是安装后的系统截图。

## 新图标

```text
Use case: logo-brand.
Asset type: final production macOS app icon artwork for LocalProject, a local developer project and Git repository manager.
Primary request: create one beautifully resolved new app icon, calm, contemporary, premium native macOS utility quality. The existing icon is too busy and too heavy; make a clean highly readable replacement.
Subject: a single large solid jade/teal folder silhouette, with one subtly offset darker teal rear folder tab visible to suggest multiple repositories. On the front folder face, a single crisp ivory Git branch glyph with exactly three circular terminal nodes, a vertical trunk and a rounded branch extending to upper right. Integrate the glyph into the folder face, balanced, broad strokes, optically centered. No more nested outlines, no separate badges. Main folder silhouette is about 65 percent of canvas width and 55 percent of height, centered vertically and horizontally. Folder gently rounded corners, coherent elegant geometry.
Style: beautifully crafted restrained dimensional macOS app artwork, frontal near-orthographic view, satin ceramic teal material, very subtle upper-left soft lighting and contact depth between the two folder layers, crisp readable silhouette at 32px. No noisy textures. Teal stays rich and sophisticated, not fluorescent.
Background: solid full-bleed opaque near-white porcelain square, extremely subtle cool mint tonal variation. Background reaches all four canvas edges. This is input artwork that macOS will mask: DO NOT draw an outer rounded-square tile, perimeter bevel, outline, transparent margin, exterior drop shadow or surrounding presentation background.
Composition: exactly one square 1024 by 1024 image, no layout sheet or examples. Safe space roughly 16 percent around main symbol. Strong small-scale clarity.
Constraints: no text, letters, watermark, logos of other products, gradients turning blue or purple, black background, thick white folder outline, neon, overly glossy glass, heavy 3D extrusion, perspective tilt. Deliver only the actual square icon artwork.
```

## 补齐不透明底色

```text
Use case: precise-object-edit. Input image is the new LocalProject icon artwork. Keep the teal folder and ivory Git symbol exactly as they are, same position and proportions. Change ONLY the empty transparent background: replace ALL transparent area with an opaque solid porcelain-white background, RGB #F3F6F5, extending fully to all four square corners and edges. Every pixel must be opaque including corners. This is a square source texture for a macOS system-masked application icon. No rounded outer corners, no outer border, no checkerboard, no surrounding space. Do not remove the background again. Preserve folder silhouette, colors, front view, Git nodes and subtle surface depth. Export as a single square image with a VISIBLE SOLID LIGHT GRAY-WHITE BACKGROUND.
```

## 系统样式模拟

```text
undefined
```

